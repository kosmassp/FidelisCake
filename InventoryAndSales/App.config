<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <!--
      Which database this installation runs on: SqlServer (default), PostgreSql or Sqlite.
      Changing it means changing ConnectionString below to match. Nothing else - the providers for
      all three are installed alongside the application and are loaded on demand.
    -->
    <add key="DatabaseProvider" value="Sqlite"/>

    <!-- SQL Server -->
    <!--<add key="ConnectionString" value="Server=localhost\SQLEXPRESS;Database=SalesInventory;User Id=sa;Password=kosmas;"/>-->

    <!-- PostgreSql:
    <add key="ConnectionString" value="Host=localhost;Database=salesinventory;Username=postgres;Password=changeme;"/>
    -->

    <!--
      Sqlite - a single file, so nothing to install and nothing to administer.
      Keep |DataDirectory|: it resolves next to the executable, whereas a bare relative path would
      follow the working directory, which the file dialogs move.
    -->
    <add key="ConnectionString" value="Data Source=|DataDirectory|\SalesInventory.db;Version=3;"/>

    <!--
      Address of the text file that says what the current release is - version.txt in the GitHub
      repository. Read once, to seed the UPDATE_MANIFEST_URL setting on a database that does not
      have it yet; after that the database value is what counts, so editing this again changes
      nothing.

      Remove the line entirely and the application still checks the official address above - it is
      built in. To switch update checking off on an installation, keep the line with an empty
      value. A Google Drive sharing link also works here; it is turned into a direct download
      automatically.

      The file it points at looks like this (the release procedure is in RELEASING.md):

        Version: 1.0.1.4
        Drive:   https://github.com/kosmassp/FidelisCake/releases
        File:    https://github.com/kosmassp/FidelisCake/releases/download/v1.0.1.4/InventoryAndSales.zip
        Notes:   Perbaikan laporan

      Drive is the page a person opens; File is the release ZIP the application installs by
      itself. Without a File line an update is only announced, never installed.
    -->
    <add key="UpdateManifestUrl" value="https://raw.githubusercontent.com/kosmassp/FidelisCake/master/version.txt"/>

    <!--<add key="PrinterName" value="Foxit Reader PDF Printer" />-->
    <add key="PrinterName" value="Microsoft Print to PDF"/>
    <!--<add key="PrinterName" value="EPSON TM-U220 Receipt" />-->
  </appSettings>

  <startup><supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.6"/></startup>
</configuration>
